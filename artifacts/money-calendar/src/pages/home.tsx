import type { FormEvent } from 'react';
import { useState } from 'react';
import { ArrowLeft, ArrowRight, Check, CircleAlert, Home as HomeIcon, Info, Loader2, Sparkles } from 'lucide-react';
import { Link, useLocation } from 'wouter';
import { useCompareRentVsBuy, type RentVsBuyResult } from '@workspace/api-client-react';

const money = (value: number) => new Intl.NumberFormat('en-AE', { maximumFractionDigits: 0 }).format(Math.round(value));
const moneyShort = (value: number) => `AED ${money(value)}`;
const initialHome = { monthlyRent: 9000, homePrice: 1500000, savings: 220000, yearsToStay: 5, annualIncome: 300000, existingInstallments: 3500, familyPlans: 'Likely relocating in 5 years' };

export default function HomePage() {
  const compare = useCompareRentVsBuy();
  const [, setLocation] = useLocation();
  const [form, setForm] = useState(initialHome);
  const [result, setResult] = useState<RentVsBuyResult>();
  const [stayingPut, setStayingPut] = useState(false);
  const setField = (key: keyof typeof initialHome, value: string) => setForm((current) => ({ ...current, [key]: key === 'familyPlans' ? value : Number(value) }));
  const submit = (event: FormEvent) => { event.preventDefault(); setStayingPut(false); compare.mutate({ data: form }, { onSuccess: setResult }); };
  const sendToCalendar = () => { window.sessionStorage.setItem('money-calendar-home-plan', result?.headline ?? 'Home decision'); setLocation('/'); };
  return <div className="space-y-7">
    <div className="reveal flex items-center gap-3"><Link href="/" className="grid size-9 place-items-center rounded-full border border-border bg-card text-muted-foreground transition-colors hover:text-primary" data-testid="link-back-home"><ArrowLeft className="size-4" /></Link><div><p className="font-mono-data text-[10px] uppercase tracking-[.16em] text-muted-foreground">Decision 02 / home</p><h1 className="font-display text-4xl leading-none text-primary md:text-5xl">Rent or buy?</h1></div></div>
    <div className="grid gap-5 lg:grid-cols-[.85fr_1.15fr]">
      <section className="reveal reveal-delay-1 rounded-2xl border border-border bg-card p-5 shadow-sm md:p-6" data-testid="section-home-form"><div className="mb-6 flex items-start justify-between"><div><p className="text-sm font-medium">Let the horizon do the talking.</p><p className="mt-1 text-xs leading-5 text-muted-foreground">A home is a cash decision, a mobility decision, and a family decision. Bring all three.</p></div><div className="grid size-10 place-items-center rounded-xl bg-secondary"><HomeIcon className="size-5 text-primary" /></div></div>
        <form onSubmit={submit} className="space-y-4">
          <div className="grid grid-cols-2 gap-3"><label className="block"><span className="mb-1.5 block text-xs font-semibold">Monthly rent <span className="font-normal text-muted-foreground">(AED)</span></span><input value={form.monthlyRent} onChange={(e) => setField('monthlyRent', e.target.value)} type="number" min="0" className="h-11 w-full rounded-xl border border-input bg-background px-3 font-mono-data text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-monthly-rent" /></label><label className="block"><span className="mb-1.5 block text-xs font-semibold">Home price <span className="font-normal text-muted-foreground">(AED)</span></span><input value={form.homePrice} onChange={(e) => setField('homePrice', e.target.value)} type="number" min="0" className="h-11 w-full rounded-xl border border-input bg-background px-3 font-mono-data text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-home-price" /></label></div>
          <div className="grid grid-cols-2 gap-3"><label className="block"><span className="mb-1.5 block text-xs font-semibold">Savings <span className="font-normal text-muted-foreground">(AED)</span></span><input value={form.savings} onChange={(e) => setField('savings', e.target.value)} type="number" min="0" className="h-11 w-full rounded-xl border border-input bg-background px-3 font-mono-data text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-home-savings" /></label><label className="block"><span className="mb-1.5 block text-xs font-semibold">Stay for <span className="font-normal text-muted-foreground">(years)</span></span><input value={form.yearsToStay} onChange={(e) => setField('yearsToStay', e.target.value)} type="number" min="1" max="30" className="h-11 w-full rounded-xl border border-input bg-background px-3 font-mono-data text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-years-to-stay" /></label></div>
          <div className="grid grid-cols-2 gap-3"><label className="block"><span className="mb-1.5 block text-xs font-semibold">Annual income <span className="font-normal text-muted-foreground">(AED)</span></span><input value={form.annualIncome} onChange={(e) => setField('annualIncome', e.target.value)} type="number" min="0" className="h-11 w-full rounded-xl border border-input bg-background px-3 font-mono-data text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-annual-income" /></label><label className="block"><span className="mb-1.5 block text-xs font-semibold">Existing payments <span className="font-normal text-muted-foreground">(AED/mo)</span></span><input value={form.existingInstallments} onChange={(e) => setField('existingInstallments', e.target.value)} type="number" min="0" className="h-11 w-full rounded-xl border border-input bg-background px-3 font-mono-data text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-existing-payments" /></label></div>
          <label className="block"><span className="mb-1.5 block text-xs font-semibold">Family plans</span><input value={form.familyPlans} onChange={(e) => setField('familyPlans', e.target.value)} type="text" className="h-11 w-full rounded-xl border border-input bg-background px-3 text-sm outline-none focus:ring-4 focus:ring-primary/30" data-testid="input-family-plans" /></label>
          <div className="flex flex-col gap-2 pt-2"><button disabled={compare.isPending} type="submit" className="flex h-11 items-center justify-center gap-2 rounded-xl bg-primary px-4 text-sm font-semibold text-primary-foreground transition-transform hover:-translate-y-0.5 disabled:opacity-60" data-testid="button-compare-home">{compare.isPending ? <Loader2 className="size-4 animate-spin" /> : <ArrowRight className="size-4" />} Compare my options</button><button type="button" onClick={() => { setStayingPut(true); setResult(undefined); }} className={`flex h-10 items-center justify-center gap-2 rounded-xl border text-xs font-medium ${stayingPut ? 'border-[hsl(var(--chart-3)/.4)] bg-[hsl(var(--chart-3)/.1)] text-[hsl(var(--chart-3))]' : 'border-border text-muted-foreground hover:bg-muted'}`} data-testid="button-home-do-nothing">{stayingPut && <Check className="size-3.5" />} Do nothing for now</button></div>
          {compare.isError && <p className="flex gap-2 text-xs text-destructive" data-testid="status-home-error"><CircleAlert className="size-4 shrink-0" />The comparison didn't complete. Check the figures and try again.</p>}
        </form>
      </section>
      <div className="reveal reveal-delay-2">{result ? <section className="rounded-2xl border border-border bg-card p-5 shadow-sm md:p-7" data-testid="section-home-result"><span className="inline-flex items-center gap-2 rounded-full bg-accent/20 px-3 py-1.5 font-mono-data text-[10px] uppercase tracking-[.12em] text-primary"><span className="size-1.5 rounded-full bg-accent" />{result.verdict.replaceAll('-', ' ')}</span><h2 className="mt-4 max-w-xl font-display text-4xl leading-[.98] text-primary md:text-5xl" data-testid="text-home-headline">{result.headline}</h2><div className="mt-7 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-border bg-background p-4"><p className="font-mono-data text-[10px] uppercase tracking-[.14em] text-muted-foreground">Day-one cash</p><p className="mt-2 text-xl font-medium">{moneyShort(result.dayOneCash)}</p></div><div className="rounded-xl border border-border bg-background p-4"><p className="font-mono-data text-[10px] uppercase tracking-[.14em] text-muted-foreground">Owning / month</p><p className="mt-2 text-xl font-medium">{moneyShort(result.monthlyOwning)}</p></div><div className="rounded-xl border border-border bg-background p-4"><p className="font-mono-data text-[10px] uppercase tracking-[.14em] text-muted-foreground">Break-even</p><p className="mt-2 text-xl font-medium">Year {result.breakEvenYear}</p></div></div><div className="mt-5 rounded-xl bg-secondary/60 p-4"><div className="mb-3 flex items-center gap-2 text-sm font-semibold"><Sparkles className="size-4 text-primary" />The flip factor</div><p className="text-xs leading-5 text-muted-foreground">{result.flipFactor}</p></div><div className="mt-5 space-y-2">{result.scenarios.map((scenario) => <div key={scenario.label} className="flex items-center justify-between border-b border-border/70 py-2.5 text-xs last:border-0"><span className="text-muted-foreground">{scenario.label}</span><span className={`font-mono-data ${scenario.netPosition >= 0 ? 'text-[hsl(var(--chart-3))]' : 'text-destructive'}`}>{scenario.netPosition >= 0 ? '+' : '−'}{moneyShort(Math.abs(scenario.netPosition))}</span></div>)}</div><button onClick={sendToCalendar} className="mt-6 flex h-11 w-full items-center justify-center gap-2 rounded-xl bg-primary text-sm font-semibold text-primary-foreground transition-transform hover:-translate-y-0.5" data-testid="button-send-home-to-calendar"><Check className="size-4" />Hold this commitment in my calendar</button><p className="mt-2 text-center text-[11px] text-muted-foreground">This does not change your calendar. It saves the decision for your review.</p><div className="mt-6 border-t border-border pt-4"><p className="flex items-center gap-2 text-xs font-semibold"><Info className="size-3.5 text-primary" />Assumptions</p><ul className="mt-2 grid gap-1 sm:grid-cols-2">{result.assumptions.map((item) => <li key={item} className="text-xs leading-5 text-muted-foreground">· {item}</li>)}</ul></div></section> : <div className="flex min-h-[440px] flex-col justify-between rounded-2xl border border-dashed border-border bg-card/60 p-6" data-testid="empty-home-result"><div><div className="mb-5 grid size-12 place-items-center rounded-2xl bg-secondary"><HomeIcon className="size-6 text-primary" /></div><h2 className="font-display text-4xl leading-[.98] text-primary">There is more than<br /><em>one kind of home.</em></h2><p className="mt-4 max-w-sm text-sm leading-6 text-muted-foreground">We will compare the monthly life, the cash you hand over on day one, and the cost of keeping your options open.</p></div><div className="flex items-start gap-3 border-t border-border pt-5 text-xs leading-5 text-muted-foreground"><Info className="mt-0.5 size-4 shrink-0 text-primary" /><span>Numbers help. Your horizon and family plans matter just as much.</span></div></div>}</div>
    </div>
  </div>;
}
